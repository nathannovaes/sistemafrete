{% extends 'base.html.twig' %}

{% block title %}Quotations{% endblock %}

{% block body %}
    <h1>Quotations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quotation.id }}</td>
            </tr>
            <tr>
                <th>Service_code</th>
                <td>{{ quotation.serviceCode }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('quotations_index') }}">back to list</a>

    <a href="{{ path('quotations_edit', {'id': quotation.id}) }}">edit</a>

    {{ include('quotations/_delete_form.html.twig') }}
{% endblock %}
